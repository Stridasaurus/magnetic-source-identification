# Python 3.12. Google Colab already provides torch, scikit-learn, numpy, pandas,
# matplotlib, and scipy; the notebooks only pip-install optuna + tqdm at runtime.
# This file pins the full environment for local / reproducible runs.
torch>=2.4,<2.5
optuna>=4.0,<5.0
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
scipy>=1.13
tqdm>=4.66
